#include <stdio.h>

int main() {
    int N, X[N], number, number2;
    scanf("%d", &N);
    scanf("%d", &X[N]);

        if (N>10 && N<20){
            number++;
        }
        else{
            number2++;
        }

    printf("%d in\n%d out\n", number, number2);

    return 0;
}
