#include <stdio.h>

int main() {
    int numero, n;
    int centena, C;
    int dezena, D;
    int unidade, U;
    int dez, dois, um;

    scanf("%d", &numero);

    for(i=0, i>=100, i++);
        centena = numero/100;
        C = numero - centena*100;
            for(i=0, i<100, i++);
                dezena = C/20;
                D = C - dezena*20;
                    if(D<20){
                        dez = D - 10;
                    }
                        for(i=0, i>5, i++);
                            unidade = dez / 5;
                            U = dez - unidade;
                                for (i=0, i<5, i++);
                                    dois = U/2


    printf("%d\n%d nota(s) de R$ 100,00 ",salary);


    return 0;
}
